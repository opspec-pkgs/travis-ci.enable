name: release
description: releases the package
inputs:
  version:
    string:
      constraints: { format: semver }
  dockerUsername:
    string: {}
  dockerPassword:
    string: {}
  pkg:
    dir:
      default: .
run:
  serial:
    - op:
        pkg: { ref: build }
        inputs:
          version:
          dockerUsername:
          dockerPassword:
          pkg: